project_id = "bgmbignfyxdpvrhmrwls"

[functions.get-google-reviews]
verify_jwt = false

[functions.notify-gift-order]
# Called from the public /gift form with no logged-in session — only the anon
# key is available, not a user JWT. Auth is unnecessary anyway: the function
# validates its own body and uses service_role internally.
verify_jwt = false

[functions.send-gift-order-email]
verify_jwt = true